# Classical ESN baseline under the two-stage search-then-holdout protocol. Global
# encoding, over the same window the quantum holdout studies use: n <= 6 and d <= 5, what
# Figure 2 plots; see README.md.
name: optuna_mixing_capacity_esn
axes:
  - n: {values: [2, 3, 4, 5, 6]}
  - d: {values: [2, 3, 4, 5]}
  - encoding_mode: {values: [dense]}

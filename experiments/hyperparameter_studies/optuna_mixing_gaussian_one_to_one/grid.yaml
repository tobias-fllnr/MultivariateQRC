# optuna_mixing_gaussian_one_to_one under the two-stage search-then-holdout protocol,
# over the window Figure 2 plots, n <= 6 and d <= 5; see README.md.
name: optuna_mixing_capacity_qrc_gaussian
axes:
  - n: {values: [2, 3, 4, 5, 6]}
  - d: {values: [2, 3, 4, 5]}
  - encoding_mode: {values: [one_to_one]}
filter: "n >= d"
